import React from "react";
import config from '../config.js';
const Lessons = ({ groupId }) => {
    return (
        <div>
            <h2>Lekcije</h2>
        </div>
    );
};

export default Lessons;
